{
  "name": "jyothi-lab-root",
  "version": "1.0.0",
  "description": "Diagnostic Lab Management Software Root",
  "scripts": {
    "install-all": "npm install --prefix client --legacy-peer-deps && npm install --prefix server --legacy-peer-deps",
    "dev": "npx -y concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\""
  }
}
